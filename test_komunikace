from rak811.rak811_v3 import Rak811
import time

lora = Rak811()
lora.hard_reset()
lora._serial.device = "/dev/ttyAMA0"  # <-- Takhle se to nastavuje v této verzi
lora._serial.open()

time.sleep(2)

lora.set_config('lora:work_mode:0')
print("Komunikace OK!")
lora.close()
